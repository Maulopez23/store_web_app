import 'package:flutter/material.dart';

class VendorsScreen extends StatelessWidget {
  const VendorsScreen({super.key});

  static const String screenRoute = "VendorsScreen";

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Vendors'),
    );
  }
}